# Port the API listens on. Defaults to 8080.
PORT=8080

# Comma-separated list of origins allowed to call this API.
# Leave unset for local development (any origin is allowed).
# ALLOWED_ORIGIN=https://your-frontend.vercel.app
